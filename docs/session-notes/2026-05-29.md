# Session Notes — 2026-05-29

**Focus:** Project kickoff. Defined RYAA, locked foundational decisions, started the model-agnostic provider layer.

## What we decided
- **Product:** RYAA, a safe/automated academic assistant for uni students. Email drafting, study suggestions, Outlook calendar scheduling. CLI first, Frutiger-Aero popup UI later.
- **First slice:** Calendar scheduling (extends the chaining example; de-risks OAuth early).
- **Model-agnostic:** hand-rolled provider layer, not LiteLLM.
- **Outlook situation:** Brandon has school 365 but is unsure whether app registration is allowed → we adopted a **stub-first / two-track** plan so the Azure unknown doesn't block coding.

## What we built
- Created `ryaa/` package skeleton: `providers/ skills/ safety/ tools/`.
- Claude wrote `ryaa/providers/base.py` — the contract:
  - `Message` Pydantic model (neutral role/content shape).
  - `LLMProvider` Protocol with `complete()` (text) and `structured()` (Pydantic-typed) — both `model=` keyword-only/optional.
  - Uses `TypeVar T` bound to BaseModel + `@runtime_checkable` Protocol.
- Claude wrote `ryaa/providers/openai_provider.py` as a **stub with 3 TODOs** for Brandon:
  - `_to_openai()` — convert `Message`s → OpenAI dicts.
  - `complete()` — wrap `chat.completions.create` (handle None content).
  - `structured()` — wrap `beta.chat.completions.parse` (raise if `parsed is None`).

## Brandon's action items
1. **Track B:** fill the 3 TODOs in `openai_provider.py`; smoke-test with the throwaway `scratch_test.py` snippet (complete + structured), run in conda env `ryaa`.
2. **Track A:** check portal.azure.com → App registrations → New registration on his **school** account. Report: created OK (+ client/tenant IDs) OR locked/admin-approval-required.

## What we shipped (rest of the day)
- ✅ **Provider layer DONE & verified** — Brandon implemented `_to_openai`/`complete`/`structured`. Learned: pure components vs app-owns-config, fail-fast on None, check-the-value-you-return, `cast` vs `# type: ignore`, `Literal` for closed sets. mypy + smoke test green.
- ✅ **Calendar skill DONE & verified** (`ryaa/skills/calendar.py`) — `CalendarSkill(provider)` via dependency injection; 3-stage chain (extract → GATE@0.7 → parse → confirm) all on `provider.structured()`. Real `datetime` + `_date_context()` fixed the hallucinated-year bug ("next Tuesday" → correct 2026 date). Verified: valid event parses, non-calendar → None.
- ✅ **Tooling overhaul** — dropped flake8+Black+autopep8+isort+pylint, standardized on **Ruff** (lint+format+imports, one config in `pyproject.toml`). Uninstalled the redundant VSCode/Cursor extensions, installed `charliermarsh.ruff`. Added **pytest** + config (unit vs `@pytest.mark.integration` split, integration excluded by default).
- ✅ **Decisions:** sync + threads (NOT async — async is contagious; only guardrails need concurrency → ThreadPoolExecutor; `asyncio.to_thread` bridges later if a server arrives).
- ✅ **DESIGN.md written** — full product/architecture doc. Added **Todos** (needs new persistence layer), **Morning Brief** (orchestrator-workers synthesis), **Dream Mode** (overnight read-only aggregation, stages proposals, commits nothing — purest "Yielding"). 4 open decisions logged (persistence, scheduling, email-read scope, todo source).
- 🔨 **Safety layer IN PROGRESS** — Claude gave the spec for `ryaa/safety/guardrails.py` (parallel + fail-closed); Brandon is writing it himself (new rule: Brandon writes ALL RYAA code, Claude specs/reviews — see [[brandon-working-style]]).

## 📋 Tomorrow's todos
1. **Write `tests/conftest.py` → `FakeProvider`** (satisfies `LLMProvider`; knobs: `text`, `structured_return`, `raises`). The `raises=True` mode is what lets us test fail-closed.
2. **Finish `ryaa/safety/guardrails.py`** — the two `_check_*` methods + the `validate()` aggregation (submit both before reading either; fail closed).
3. **Write the first unit tests** — calendar gate (conf 0.5 → None), guardrails (unsafe → rejected, raising fake → rejected, both-pass → valid).
4. **Confirm gate** — the final "yield" before any outside-world action.
5. **(Stretch) Azure app-registration check** — still the external long pole for real Outlook.
6. **💡 IDEA: morning Mac notification** — wire a daily `osascript display notification` (via `launchd`) that eventually fires the RYAA **Morning Brief**. Proved the capability works today (fired a test notification). Decide a time + whether to set up the launchd job. Ties directly into Dream Mode → Morning Brief.

## Notes / reminders
- Teaching rhythm UPDATED: **Brandon writes all RYAA code himself**; Claude specs/designs/reviews, does NOT write the .py modules (tooling/config exceptions OK).
- Don't jump to real Outlook/OAuth yet — finish the testable scheduling brain + safety layer against the stub first.
